import React from "react";
import {
    View,
    Text
} from "react-native";

const LoginScreen = ({
    navigation
}) => {
    <View style={{
        justifyContent: "center",
        alignItems: "center"
    }}>
        <Text>Here! You're on Login</Text>
    </View>
}

export default LoginScreen;